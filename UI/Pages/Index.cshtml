@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

<div class="flex flex-col items-center justify-center min-h-screen">
    <h1 class="text-3xl font-bold mb-4">Welcome!</h1>
    <p class="text-lg">You are logged in.</p>
    <p class="mt-2">User: @User.Identity?.Name</p>
</div>
